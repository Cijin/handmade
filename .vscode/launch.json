{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Zig (GDB)",
            "type": "gdb",
            "request": "launch",
            "target": "${workspaceFolder}/zig-out/bin/handmade",
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "zig build",
            "valuesFormatting": "parseText"
        }
    ]
}